import BridgeBoard from "./BridgeBoard"; // 아래 새로 만든 BridgeBoard.jsx를 로드합니다.

function App() {
  return (
    <div className="App">
      {/* BridgeBoard를 메인 대시보드로 즉시 화면에 가동 */}
      <BridgeBoard />
    </div>
  );
}

export default App;